# Held payload launch contract

Payload access remains forbidden until an independent source auditor verifies
this package and freezes an integration patch against the current global
STRATA encoder and a separately implemented decoder.

A permitted experiment must, in one fresh run:

1. authenticate the original BF16 source, block map, base encoder, BEC wrapper,
   frozen semantic auditor, package manifest, and launch script;
2. call the pinned BEC hook for every current block/level and record its exact
   selected count `K` and flag hash;
3. generate the normative RM-ordered replacement and prove the same `K`;
4. use only the implemented `current_random` frozen-value coset;
5. rerun all six SC passes from source; no old decisions, frequencies,
   reconstruction, residual, arithmetic stream, or score may be reused;
6. emit one literal versioned expert-local packet with every selector and byte
   charged;
7. independently decode, regenerate probabilities, consume the packet exactly,
   reconstruct all source-domain values, and calculate FP64 SSE/energy;
8. canonically re-encode the complete packet and require byte identity;
9. repeat the complete selection and coding procedure for matched Gaussian and
   structure-destroying controls;
10. report physical bytes and routed cold-page reads.  No count, NLL, projected
    rate, or overlap statistic may substitute for emitted bytes.

Promotion additionally requires the literal rate to be in `[2.15,2.5]` bpw,
the codec to satisfy the repository's universal SwiGLU-MoE and `<2x` routed
read contracts, and a separately authenticated result audit.  This source
package grants none of those outcomes.

