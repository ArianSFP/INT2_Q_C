# PAIRPATH-P2 r3 checkpoint

Status: **SEALED SOURCE-ONLY HOLD PENDING INDEPENDENT HOSTILE AUDIT**.

Resolved from the r2 audit:

- finite Up and Down fits share the exact global Up/Down multiplier;
- every heuristic joint solution dominates the supplied independent candidate
  under the same joint objective;
- low heuristic gain is explicitly non-authoritative because global optimality
  remains unproved;
- literal decoding validates and causally replays the full tree descriptor.

Nine deterministic KATs pass. The package remains payload-blind and cannot
authorize local GPU execution, Qwen access, promotion, or a hard kill.

Next permitted action: independent hostile audit of the exact manifest. Any
model-data run requires a new, separately reviewed, one-use capability.
