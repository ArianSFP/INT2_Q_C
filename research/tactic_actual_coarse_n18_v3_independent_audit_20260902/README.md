# Independent source audit — TACTIC actual-coarse N18 v3

This directory is owned by the independent audit. It does not modify or seal
the producer directory. `producer_inventory.json` binds the exact stopped
14-file producer tree. `hostile_audit.py` performs independent authentication,
integer layout checks, target-contract counterexamples, and static format and
runtime-trust checks without importing numeric dependencies or opening model
data.

The producer's verifier and hostile tests are replayed separately on POSIX by
executing the authenticated `immutable_bootstrap.py` bytes through a held
`/proc/self/fd/N`. Any RunPod receipts and the final verdict are evidence of a
source-only audit; they are not payload, runtime, CUDA, codec-quality, or
result authority.
