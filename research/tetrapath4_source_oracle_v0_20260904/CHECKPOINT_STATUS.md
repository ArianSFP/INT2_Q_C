# TETRAPATH-4 v0 checkpoint status

Status: **UNSEALED SOURCE-ONLY — NO PAYLOAD AUTHORITY**

Implemented:

- exact four-variable, four-label distortion enumeration;
- equal-flexibility independent, all three 2+2 pairings, Chow–Liu tree,
  all-pairwise maximum-entropy, sparse Gray-parity log-linear, three
  equal-fiber Gray–Wyner-local, and full 256-state families;
- identical deterministic symmetry-closed multistarts;
- one exact rational global RD multiplier per sweep point;
- lower convex envelopes and both equal-rate and equal-MSE comparisons;
- an explicit free-table/free-time-sharing kill-only ledger;
- XOR synergy, balanced IID, exhaustive tuple, exhaustive tiny assignment,
  and exhaustive labelled-tree tests;
- a constructive XOR common/private fiber ledger with `0.25 bpw` saving and
  `4/3` ideal logical read amplification (not a page-rounded packet);
- an unconditional payload-execution failure gate.

Not implemented or authorized:

- any real model access or result;
- any GPU, local-machine, remote-machine, or network runner;
- finite table/model/selector/time-sharing bytes;
- held-out whole-layer controls;
- a literal packet or independent decoder;
- sealing, manifest, deployment, or execution authority.

Promotion rule: none.  `>=0.045 bpw` only permits a separately audited,
finite follow-up.  `<0.045 bpw` kills the bounded memoryless four-way branch.
