# FOSP auxiliary gross necessary-bound screen v0

Status: **development-panel scientific falsification only**. This is not the
FOSP-v4 deployment path and is not a validation, pinned-panel, finite-codec, or
target-achievement result.

The screen uses the exact frozen FOSP-v3/v4 `3 x 3` predecessor regression on
two already-open auxiliary layer-3 Qwen experts. For every target neuron it
grants the best distinct predecessor independently. That many-to-one relaxed
reuse is strictly more permissive than any legal permutation path, so its raw
Qwen `s` is a deterministic necessary bound for this FOSP family. If it misses
the side-adjusted gross requirement `0.1858070514584381 bpw`, controls, cycle
cover, FP16 path replay, and residual encoding cannot rescue the architecture
and are skipped.

The CuPy implementation imports the byte-identical frozen scientific oracle,
checks all six BF16 source hashes, evaluates all `2 * 768 * 767` nonself pairs
in binary64, and records physical side/read accounting. It opens no fresh
validation or pinned-panel expert.

Run on the supplied RunPod with an absent output directory:

```bash
CUDA_VISIBLE_DEVICES=0 /workspace/int2-cupy-venv/bin/python -B \
  gross_screen.py \
  --workspace /workspace/INT2__compression \
  --output /var/tmp/fosp_aux_gross_v0_result
```

A survivor is only authorization to design and audit the legal-FP16 auxiliary
follow-up. A miss is scoped to the exact frozen FOSP-v3/v4 predictor family.

